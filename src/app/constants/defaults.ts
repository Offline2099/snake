import { Constraints } from '../types/level';

export const DEFAULT_CONSTRAINTS: Constraints = {
  xmin: 0,
  xmax: 31,
  ymin: 0,
  ymax: 31,
  obstacles: []
}

export const DEFAULT_STEP_TIME: number = 150;